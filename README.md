# lua-build
Lua for windows build using makefile and Clang/LLVM compiler

# Dependencies
- Clang/LLVM
- Windows SDK
- gmake

# How to build
```
cd lua
mkdir build
make buildware
```

# Other
This build are for version `lua 5.4.5`
